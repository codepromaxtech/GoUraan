import { ConfigService } from '@nestjs/config';
export declare class TranslationService {
    private configService;
    private readonly logger;
    private translations;
    private supportedLocales;
    constructor(configService: ConfigService);
    private loadTranslations;
    translate(key: string, locale?: string, params?: Record<string, any>): string;
    private getNestedValue;
    getSupportedLocales(): string[];
    isLocaleSupported(locale: string): boolean;
    getTranslations(locale: string): any;
    translateBookingStatus(status: string, locale?: string): string;
    translateBookingType(type: string, locale?: string): string;
    translatePaymentStatus(status: string, locale?: string): string;
    formatCurrency(amount: number, currency: string, locale?: string): string;
    formatDate(date: Date, locale?: string, options?: Intl.DateTimeFormatOptions): string;
    formatNumber(number: number, locale?: string): string;
    getEmailTemplate(templateKey: string, locale?: string): any;
    getSmsTemplate(templateKey: string, locale?: string): string;
    getValidationMessage(field: string, rule: string, locale?: string): string;
    getErrorMessage(errorCode: string, locale?: string): string;
    getSuccessMessage(actionKey: string, locale?: string): string;
    pluralize(key: string, count: number, locale?: string): string;
    isRTL(locale: string): boolean;
    getTextDirection(locale: string): 'ltr' | 'rtl';
    reloadTranslations(): Promise<void>;
    getTranslationStats(): any;
    private countKeys;
    suggestTranslationKey(text: string): string;
}
